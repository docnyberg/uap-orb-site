{
  "name": "uap-orb-site",
  "private": true,
  "version": "1.0.0",
  "description": "Netlify deployable site with PayPal-verified Pro features",
  "main": "index.js",
  "scripts": {
    "serve": "netlify dev",
    "deploy": "netlify deploy --prod --dir public --functions public/netlify/functions"
  },
  "dependencies": {
    "jsonwebtoken": "^9.0.2",
    "node-fetch": "^2.6.12"
  }
}
